#!/bin/bash

cd {{ webgoat_basedir }}

nohup TZ={{ webgoat_timezone }} java -Dfile.encoding=UTF-8 -jar webgoat.jar > webgoat.log &

sleep 20
#timeout 30 bash -c 'until printf "" 2>>/dev/null >>/dev/tcp/localhost/{{ webgoat_port }}; do sleep 1; done'

nohup TZ={{ webgoat_timezone }} java -jar webwolf.jar > webwolf.log &
sleep 5